apiVersion: v1
kind: PersistentVolumeClaim
metadata:
  name: smb-avatars-pvc
spec:
  accessModes:
    - ReadWriteMany
  resources:
    requests:
      storage: 10Gi
  storageClassName: smb-media
  volumeName: smb-avatars-pv   