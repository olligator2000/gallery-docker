apiVersion: v1
kind: Service
metadata:
  labels:
    io.kompose.service: web-production
  name: web-production
spec:
  type: NodePort
  ports:
    - name: "8001"
      port: 8001
      targetPort: 8000
      nodePort: 30081
  selector:
    io.kompose.service: web-production