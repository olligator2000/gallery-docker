apiVersion: v1
kind: Service
metadata:
  labels:
    io.kompose.service: web-staging
  name: web-staging
spec:
  type: NodePort
  ports:
    - name: "8000"
      port: 8000
      targetPort: 8000
      nodePort: 30080
  selector:
    io.kompose.service: web-staging